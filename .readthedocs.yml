# .readthedocs.yml

version: 2
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt
    - requirements: docs/requirements.txt

# Build all formats
formats: all
